package helpers

import "log"

func GetClassId(class string) (int64, error) {

	log.Printf("[helpers] getting class ID corresponding to class: %s", class)

	return 0, nil
}

func GetDesignationId(designation string) (int64, error) {

	log.Printf("[helpers] getting designation ID corresponding to designation: %s", designation)

	return 0, nil
}
